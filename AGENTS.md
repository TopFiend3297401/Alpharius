# Agent ground rules for Alpharius

- This repository lives at `/mnt/ORICO-1TB/Code-Projects/projects-personal/Alpharius`.
- "The vault" always means the `vault/` directory inside this repository. No other directory
  named vault, on this machine or any other, is relevant to your work.
- `vault/docs/` holds stable, verified, community-facing notes. `vault/dev/` holds working notes.
  Do not move a note from dev to docs unless explicitly asked.
- Work only inside this repository. Never use ssh, scp or rsync. Never read or write outside
  the repository root.
- Do not install packages unless explicitly asked.
- UK English. No emoji. No decorative headers.
