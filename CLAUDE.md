# Alpharius

Read `AGENTS.md` — it is the source of truth for agent ground rules in this repository, shared
across harnesses. Repo purpose and layout: `README.md`. Knowledge lives in `vault/` (split-brain:
`docs/` verified and community-facing, `dev/` working notes); start at `vault/Home.md`.
