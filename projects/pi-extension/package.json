{
  "name": "alpharius-pi-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Alpharius exoskeleton for Pi: shell fence, repo jail, repeat guard, anchored edit rescue, phantom-path hints. Hooks only; zero added prompt tokens.",
  "type": "module",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test": "node --test test/core.test.ts test/fence.test.ts test/hooks.test.ts",
    "smoke": "./smoke.sh"
  },
  "engines": {
    "node": ">=22.18"
  }
}
