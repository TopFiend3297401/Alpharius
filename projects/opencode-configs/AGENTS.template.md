# Agent ground rules for <PROJECT-NAME>

- This repository lives at `<ABSOLUTE-REPO-PATH>`.
- <Pin every load-bearing noun your prompts use. Example: "the vault" always means the `vault/`
  directory inside this repository; no other directory of that name, on this machine or any
  other, is relevant.>
- Work only inside this repository. Never use ssh, scp or rsync. Never read or write outside
  the repository root.
- Do not install packages unless explicitly asked.
- <House style: language variant, formatting rules, anything the model must not invent.>

Keep this file short. Every line must earn its place — a small model's attention is the
scarcest resource in the loop.
